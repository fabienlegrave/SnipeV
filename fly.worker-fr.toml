# fly.toml app configuration file generated for worker-fr-icy-night-8180 on 2025-11-26T10:55:26+01:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'worker-fr-icy-night-8180'
primary_region = 'cdg'

[build]

[processes]
  worker = 'npm start'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['worker']
  
  # Health check pour maintenir l'app active et déclencher l'initialisation
  [[http_service.checks]]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/api/health"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  processes = ['worker']
